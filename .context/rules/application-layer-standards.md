<metadata updated="2025-11-05" version="3.0.0" name="应用层规范标准">
  <keywords>应用层规范, 页面设计, 决策树, 组件拆分, 状态管理, 布局, React, TypeScript, 页面层职责</keywords>
</metadata>

# 📋 应用层规范标准

> 基于React框架的应用层开发规范，以**决策为驱动**，**示例为参考**。所有具体实现（代码、配置）统一归集至 `.context/examples/`，本规范仅定义**设计原则与决策逻辑**。

> ✅ **原则**: 规范说方向，示例给方法
> ✅ **目标**: 让团队在“该不该做”上达成共识，在“怎么做”上获得标准

## 🎯 一、整体方案决策（宏观架构）

> 判断一个业务功能是否应独立为页面，而非组件或模块。

### 1.1 独立页面决策

> 是否为独立业务域？

```
是否为独立业务域？
├─ 是
│  ├─ 是否有独立路由、权限、入口？
│  │  ├─ 是 → 创建独立页面（src/pages/命名）
│  │  └─ 否 → 作为模块嵌入其他页面（如 Dashboard）
│  └─ 是否需要专属布局或复杂状态？
│     ├─ 是 → 使用 PageContainer + 专用容器
│     └─ 否 → 使用通用布局（如 Card + Grid）
└─ 否
   ├─ 是否为单一操作流程？（如创建/编辑/预览）
   │  ├─ 是 → 使用 Modal 或 Drawer，**不创建页面**
   │  └─ 否 → 是否为数据查看类（列表/详情）？
   │     ├─ 是 → 创建详情页（src/pages/.../Detail）
   │     └─ 否 → 重构为可复用组件，非页面
```

> **决策要点**: 
> - ✅ **独立页面** 适用于: 独立权限控制、独立路由、高频访问、复杂交互
> - ✅ **弹窗/抽屉** 适用于: 轻量级、需保留上下文、高频操作
> - ✅ **嵌入模块** 适用于: 仪表盘、聚合页、低频功能
> - ❌ **禁止为创建/编辑/查看创建三个页面** —— 应统一为 `/detail?mode=create/edit/view`
> - ✅ 鼓励使用 `mode` 参数实现单页面多态（参考: [ProTable 组件模式](../examples/antd-pro-patterns/pro-table.md)）

## 🏗️ 二、页面分层决策（中观设计）

> 在确定“这是个页面”后，如何组织其内部结构？

### 2.1 页面结构分层原则

| 层级 | 职责 | 实现方式 |
|------|------|----------|
| **路由层** | 路由控制、权限校验、组件组合 | `config/routes.ts` 中指定页面路由 |
| **应用层** | 整体结构、容器、区域划分 | 使用 `PageContainer` + 业务模块 **避免重复的属性传递** |
| **模块层** | 业务功能单元（自管理数据） | `components/` 内的业务模块，**直接访问 store 和 service** |
| **原子层** | 纯 UI 元素 | `src/components/` 中的复用元素（如 `UserSelector`、`StatusBadge`） |

> ⚠️ **禁止**: 入口层处理业务数据、模块层依赖 props 传递数据、原子层包含业务逻辑

### 2.2 组件拆分决策树

> 判断一个功能该属于哪个层级

```
是否需要跨页面复用？
├─ 是 → 放入 src/components/（跨场景复用业务组件）
└─ 否
   ├─ 是否为完整独立功能模块？
   │  ├─ 是 → 放入页面 components/（高内聚独立业务模块）
   │  └─ 否 → 留在页面主入口（应用层布局控制）
   └─ 是否为页面级布局控制？
      ├─ 是 → 留在页面主入口
      └─ 否 → 考虑组件设计是否合理
```

## 🎨 三、布局方案规范（视觉与结构）

> 所有页面布局必须遵循统一视觉语言。

### 3.1 布局首选方案

- **推荐使用页面容器组件**作为页面根容器，提供标准页头、面包屑、边距
- **禁止** 手动使用 `<div>` 包裹标题、操作按钮（应使用容器组件的配置）
- **禁止** 创建3级及以上嵌套容器，所有内容应直接为容器组件的子元素
- **禁止** 附加其他排版样式，保持简洁的容器结构

> ✅ 正确: 使用页面容器包裹业务组件
> ❌ 错误: 创建过深的嵌套结构

#### 3.1.1 模板代码参考

> 所有页面容器和布局相关的模板代码已统一整合至 `.context/examples/layout-patterns/` 目录

**核心模板文件**:
- [`page-common-patterns.md`](../examples/layout-patterns/page-common-patterns.md) - 页面基础模板（整合 PageContainer、面包屑导航和业务组件状态管理）

**关键规范**:
- ✅ **标准模板**: 参考 `页面基础模板` 实现
- ✅ **面包屑集成**: 参考 `页面基础模板` 中的面包屑实现
- ❌ **禁止模式**: 参考 `禁止行为` 避免错误实现
- ❌ **嵌套限制**: 避免创建3级及以上嵌套容器

### 3.2 通用布局模式

- **标准布局**: `PageContainer` + `Module1` + `Module2`
- **列表详情布局**: `PageContainer` + `ProTable` + `SceneModal`（无独立详情页）

### 3.3 响应式与主题

- **优先使用 Tailwind CSS**：所有布局、间距、颜色、字体、对齐、响应式样式（如 `p-4`, `text-blue-600`, `flex items-center`, `sm:flex-col`）
- **谨慎使用 CSS变量**：仅用于全局主题变量（如 `--ant-primary-color`），在 `src/styles/variables.less` 中定义
- **禁止**：
  - 直接修改 Ant Design 组件内部样式（如 `.ant-btn-primary`）
- **唯一例外**：动画、伪元素、复杂渐变等 Tailwind 不支持的样式，允许在组件同目录 `index.less` 中定义，且**不定义类名**，仅使用内联样式或 `:global()` 定义

> ✅ 正确：`<Button className="bg-blue-600 hover:bg-blue-700 text-white">提交</Button>`


## 🗃️ 四、状态设计决策（数据流）

> 状态是应用的"大脑"。本节定义状态应放在何处。

### 4.1 应用层职责边界

> 应用层负责核心模块的引用和整体布局，**不负责组件属性传递**。

#### 4.1.1 应用层设计原则

| 设计原则 | 说明 | 实现策略 |
|----------|------|----------|
| **模块自管理优先** | 避免层层传递相同的业务数据 | 业务模块直接访问 store 和 service |
| **接口隔离** | 页面层不直接处理复杂业务逻辑 | 委托给专门的业务模块组件 |
| **保持简洁** | 页面层专注于布局和组合控制 | 复杂数据处理封装到 hooks 或模块 |

#### 4.1.2 应用层正确职责

| 允许行为 | 说明 | 示例 |
|----------|------|------|
| **布局组合** | 组合业务组件，控制页面结构 | `<PageContainer><SceneTable /><SceneModal /></PageContainer>` |
| **权限控制** | 页面级权限校验和重定向 | `useAccess()` 和条件渲染 |


> ✅ **推荐模式**: `<SceneTable />` 内部自行管理数据加载、store 访问、状态更新
> ❌ **冗余模式**: `<SceneTable data={data} loading={loading} onUpdate={handleUpdate} />` 重复传递 store 已有的数据

#### 4.1.3 业务组件状态管理模板

> 所有业务组件状态管理的模板代码已迁移至 `.context/examples/component-patterns/` 目录

**核心模板文件**:
- [`business-component-state-management.md`](../examples/component-patterns/business-component-state-management.md) - 业务组件自管理状态完整指南

**关键规范**:
- ✅ **自管理状态**: 参考 `业务组件自管理状态模板`
- ✅ **简洁组合**: 参考 `页面层简洁组合模板`
- ❌ **避免冗余传递**: 参考 `错误模式示例`
- ✅ **直接访问 store**: 业务组件直接使用 zustand store
- ✅ **接口隔离**: 页面层只负责组合，不处理复杂业务逻辑

### 4.2 状态分类原则

| 状态类型 | 说明 | 存放位置 | 示例 |
|----------|------|----------|------|
| **业务状态** | 跨组件共享状态,服务层数据、列表、选中项、加载状态 | **zustand store**（状态层） | `scenes`, `isLoading`, `selectedScene` |
| **UI 状态** | 组件内聚状态,弹窗可见性、表单填写状态、分页页码 | **useState**（组件内） | `modalVisible`, `editingSceneCode` |
| **服务层无状态** | API 响应结构、请求参数 | **服务层类型** | `IReqQuerySceneList`，`IResSceneDetail` |

> ⚠️ **禁止**: 将业务状态放在 `useState`，或将 UI 状态放在 `zustand`

### 4.2 类型引用规范

> 所有类型应从最权威来源导入，避免重复定义。

| 优先级 | 来源 | 用法 |
|--------|------|------|
| **1（最高）** | 服务层接口 | `import type { IReqQuerySceneList } from '@/services/llmSceneService/interface';` |
| **2（高）** | 状态层导出类型 | `import type { EvalSceneModel } from '@/stores/llmSceneStore';` |
| **3（中）** | 组件内定义 | `interface ModalProps { ... }` |

> ✅ 正确: `const requestHandler = (params: Partial<IReqQuerySceneList>) => { ... }`
> ❌ 错误: 重新定义 `interface LocalType { ... }`

### 4.3 数据模型引用规范

> 应用层引用接口数据时，始终引用对应的数据模型定义，避免重复定义或篡改数据模型。

#### 4.3.1 引用原则

| 场景 | 要求 | 示例 |
|------|------|------|
| **API 调用** | 使用服务层定义类型 | `import { IReqQuerySceneList } from '@/services/.../interface';` |
| **组件 Props** | 优先使用服务层/状态层类型 | `{ data: EvalSceneModel[] }` |
| **状态管理** | 直接使用服务层类型 | `useState<EvalSceneModel[]>([])` |

#### 4.3.2 禁止行为

- ❌ **重复定义**: 重新创建服务层已有类型
- ❌ **类型篡改**: 修改导入的类型定义
- ❌ **类型降级**: 简化为 `any` 类型
- ❌ **硬编码结构**: 组件中硬编码数据结构

#### 4.3.3 扩展规范

如需扩展服务层类型: 

1. **显式导入**: `import type { IReqCreateScene } from '@/services/...';`
2. **类型扩展**: `type SceneForm = IReqCreateScene & { confirmName?: string };`
3. **保持兼容**: 扩展字段不冲突

> ✅ **正确**: 引用服务层类型 + 合法扩展
> ❌ **错误**: 重复定义 + 使用 any + 硬编码结构

## 📋 五、项目结构与命名规范

> 所有文件按功能组织，命名清晰统一。

### 5.1 标准页面目录结构

```
src/pages/{页面名称}/
├── index.tsx                    # 页面入口（布局组合+模块协调，避免冗余属性传递）
├── components/                  # 页面私有组件（业务模块，自管理数据）
│   ├── SceneTable/              # 业务模块，直接访问 store 和 service
│   │   ├── index.tsx            # 自主管理数据加载和状态
│   │   └── index.test.tsx
│   └── SceneModal/
│       ├── index.tsx
│       └── index.test.tsx
└── utils/                       # 页面级工具函数（纯逻辑，无业务数据）
    ├── layoutHelpers.ts         # 布局辅助函数
    └── validators.ts            # 通用验证逻辑
```

> **关键变化**: 
> - **页面入口**: 主要负责组件组合，可访问 store **仅用于模块协调**
> - **业务组件**: 自主管理数据，直接访问 zustand store 和 service
> - **工具函数**: 纯逻辑处理，不涉及业务数据操作

### 5.2 文件命名规范

| 文件类型 | 命名规范 | 示例 |
|----------|----------|------|
| 页面主入口 | PascalCase | `LlmSceneManagement/index.tsx` |
| 业务组件 | 功能描述 | `SceneTable/index.tsx` |
| 工具函数 | 功能描述 | `dataTransform.ts` |

### 5.3 路由配置规范

React项目支持多种路由配置方式，所有路由配置应统一在相应配置文件中定义，遵循以下规范:

#### 5.3.1 声明式路由配置（推荐）

使用 `app/routes.ts` 进行声明式路由配置，每个路由包含URL模式和模块文件路径：

```typescript
import {
  type RouteConfig,
  route,
  index,
  layout,
  prefix,
} from "@react-router/dev/routes";

export default [
  index("./home.tsx"),
  route("about", "./about.tsx"),

  layout("./auth/layout.tsx", [
    route("login", "./auth/login.tsx"),
    route("register", "./auth/register.tsx"),
  ]),

  ...prefix("concerts", [
    index("./concerts/home.tsx"),
    route(":city", "./concerts/city.tsx"),
    route("trending", "./concerts/trending.tsx"),
  ]),
] satisfies RouteConfig;
```

**关键概念**:
- **route()**: 定义普通路由
- **index()**: 定义索引路由（默认子路由）
- **layout()**: 定义布局路由（不增加URL路径段）
- **prefix()**: 定义路由前缀（批量添加路径前缀）

#### 5.3.2 路由模块（Route Modules）

每个路由对应一个模块文件，包含数据加载和渲染逻辑：

```typescript
// 提供类型安全和推断
import type { Route } from "./+types/team";

// 数据加载函数
export async function loader({ params }: Route.LoaderArgs) {
  let team = await fetchTeam(params.teamId);
  return { name: team.name };
}

// 渲染组件
export default function Component({
  loaderData,
}: Route.ComponentProps) {
  return <h1>{loaderData.name}</h1>;
}
```

#### 5.3.3 动态参数

路径段以 `:` 开头时成为动态段：

```typescript
route("teams/:teamId", "./team.tsx"),
```

```typescript
import type { Route } from "./+types/team";

export async function loader({ params }: Route.LoaderArgs) {
  // params: { teamId: string }
}

export default function Component({
  params,
}: Route.ComponentProps) {
  params.teamId; // string 类型
}
```

#### 5.3.4 可选参数

在段末添加 `?` 可使路由段变为可选：

```typescript
route(":lang?/categories", "./categories.tsx"),
route("users/:userId/edit?", "./user.tsx"),
```

#### 5.3.5 通配符路由

以 `/*` 结尾的通配符路由会匹配所有剩余路径：

```typescript
route("files/*", "./files.tsx"),
route("*", "./catchall.tsx"), // 捕获所有未匹配路由
```

#### 5.3.6 文件系统路由

如需使用文件命名约定，可使用 `@react-router/fs-routes` 包：

```typescript
import {
  type RouteConfig,
  route,
} from "@react-router/dev/routes";
import { flatRoutes } from "@react-router/fs-routes";

export default [
  route("/", "./home.tsx"),
  ...(await flatRoutes()),
] satisfies RouteConfig;
```

> 甚至可以结合不同的路由约定方式。

### 5.4 路由参数获取规范

在需要获取路由参数时，根据项目使用的路由系统选择相应的获取方式，遵循以下规范:

- **参数获取方式**: 根据路由实现方式获取参数
- **类型安全**: 创建类型接口定义路由参数，确保类型安全
- **使用规范**: 始终检查参数是否存在，避免空值异常
- **必须处理**: 参数解析失败的情况（如非数字ID、空值等）

示例:
```typescript
// 示例：使用声明式路由配置的参数获取方式
import type { Route } from "./+types/team";

export async function loader({ params }: Route.LoaderArgs) {
  // params: { teamId: string }
}

export default function Component({
  params,
}: Route.ComponentProps) {
  params.teamId; // string 类型
}
```

> **说明**: 所有示例、模板、最佳实践代码均统一归集于: 
> **[.context/examples/antd-pro-patterns/](../examples/antd-pro-patterns/)**

<reference type="guidance">
- [架构规范标准](./architecture-standards.md)
- [视图层规范标准](./component-standards.md)
- [ProTable 组件模式](../examples/antd-pro-patterns/pro-table.md)
- [Ant Design Pro Components](https://procomponents.ant.design/)
</reference>